# Public site origin used by metadata, sitemap and robots.
# Production value lives in .env.production (committed; public URL only).
NEXT_PUBLIC_SITE_URL=https://saudtechnology.com

# Environment used to determine the build environment.
PAGES_ENVIRONMENT=github-pages
