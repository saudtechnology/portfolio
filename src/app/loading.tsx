export default function Loading() {
	return (
		<main className="flex flex-1 items-center justify-center px-8 py-24">
			<p className="text-sm tracking-[0.2em] text-mute">A carregar</p>
		</main>
	);
}
